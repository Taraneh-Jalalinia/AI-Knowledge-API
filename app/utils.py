"""
Helper functions: RAG pipeline (LlamaIndex + ChromaDB), Redis cache get/set.
"""
# TODO: RAG pipeline
# - Initialize ChromaDB vector store, LlamaIndex index (e.g. VectorStoreIndex)
# - run_rag_query(query: str) -> answer, sources

# TODO: Redis cache
# - get_cached_response(key: str) -> Optional[dict]
# - set_cached_response(key: str, value: dict, ttl: int = 3600)
