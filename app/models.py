"""
Database models (PostgreSQL/MySQL via SQLAlchemy or similar).
Define: User (for JWT auth), Document (metadata, path, embedding_status).
"""
# Example structure - use SQLAlchemy, Tortoise, or Django ORM as you prefer

# class User:
#     id, email, hashed_password, created_at

# class Document:
#     id, user_id (FK), filename, file_path, status (pending|processing|ready|failed)
#     created_at, updated_at

# TODO: Add your ORM models and engine/session setup here
